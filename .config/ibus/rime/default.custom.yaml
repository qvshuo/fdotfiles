patch:

# 输入方案
  schema_list:
    - schema: luna_pinyin

# 候选词数量
  "menu/page_size": 7

# Caps & Shift 直接上屏
  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Caps_Lock: "commit_text"
      Shift_L: "commit_code"
      Shift_R: "commit_code"
      Control_L: "noop"
      Control_R: "noop"
