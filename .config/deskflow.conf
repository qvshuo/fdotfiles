section: screens
	macmini.local:
	fedora:
end

section: aliases
end

section: links
	macmini.local:
		right = fedora(15,100)
	fedora:
		left(15,100) = macmini.local
end

section: options
	protocol = synergy
end

